# Core — Google ADK + Vertex AI
google-adk>=2.2.0
google-cloud-resource-manager>=1.17.0
google-cloud-iam>=2.23.0
google-genai>=2.8.0

# UI
streamlit>=1.58.0

# Config
python-dotenv>=1.2.2

# ⚠️ Indirect dependencies are resolved automatically by pip.
# Do not add them here — only list packages that are directly imported.
